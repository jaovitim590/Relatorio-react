{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    // A opção 'baseUrl' foi removida, pois é obsoleta (deprecated) e não é mais necessária 
    // quando 'paths' são definidos de forma relativa ao projeto (que é o padrão).
    "paths": {
      // Configuração de atalho de importação (ex: import { App } from '@/components/App')
      "@/*": ["./src/*"] 
    }
  }
}